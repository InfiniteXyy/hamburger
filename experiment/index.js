import ReactDOM from 'react-dom';
import ExampleStyleProperty from './src/styleProperty';
import ThemeBootstrap from './src/themeBootstrap';

const root = document.getElementById('root');
ReactDOM.render(ThemeBootstrap, root);
