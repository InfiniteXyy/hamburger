{
  "name": "hamburger",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "repository": "https://github.com/hamburger-js/hamburger",
  "workspaces": [
    "hamburger-*",
    "examples/*",
    "experiment"
  ],
  "author": "Yiyang Xu <xuyiyangwing@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build:cli": "cd hamburger-cli && yarn build",
    "build:lib": "cd hamburger-js && yarn build",
    "build:dsl": "cd hamburger-loader && yarn build",
    "test:lib": "cd hamburger-js && yarn test",
    "dev:lib": "cd hamburger-js && yarn dev",
    "build": "yarn build:cli && yarn build:lib && yarn build:cli && yarn build:dsl",
    "test": "yarn test",
    "dev": "yarn dev:lib",
    "playground": "cd examples/apps && yarn start"
  },
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
